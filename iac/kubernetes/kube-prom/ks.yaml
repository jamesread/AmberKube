apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: kube-prom
  namespace: flux-system
spec:
  interval: 10m
  path: ./iac/kubernetes/kube-prom/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
