apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: kube-state-metrics
  namespace: flux-system
spec:
  interval: 10m
  path: ./iac/kubernetes/kube-state-metrics/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
