[defaults]
inventory = hosts.yml
roles_path = roles
# One-line file containing the vault password (chmod 600). Not committed — see README.
vault_password_file = .vault_pass

[ssh_connection]
pipelining = True
